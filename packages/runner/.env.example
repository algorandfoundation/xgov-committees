# Env vars for the runner live in the root .env / .env.example.
# See: ../../.env.example
#
# For local dev: create ../../.env (repo root) with your values.
# For systemd: EnvironmentFile= in runner.service points to /opt/xgov-committees/.env
